specVersion: "v1beta"
serviceId: "event-manager-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "event-manager-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
